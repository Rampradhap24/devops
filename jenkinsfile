pipeline {
    agent any

    environment {
        APP_NAME = "myapp"
        DOCKER_IMAGE = "myapp:latest"
    }

    stages {
        stage('Clone Repo') {
            steps {
                echo "Cloning repository..."
                git 'https://github.com/Rampradhap24/devops.git'
            }
        }

        stage('Build Docker Image') {
            steps {
                echo "Building Docker image..."
                sh 'docker build -t $DOCKER_IMAGE .'
            }
        }

        stage('Run Docker Container') {
            steps {
                echo "Running Docker container on port 5500..."
                sh '''
                docker rm -f $APP_NAME || true
                docker run -d -p 5500:5500 --name $APP_NAME $DOCKER_IMAGE
                '''
            }
        }
    }

    post {
        success {
            echo "✅ Application is running locally on http://localhost:5500"
        }
        failure {
            echo "❌ Build or container run failed!"
        }
    }
}
